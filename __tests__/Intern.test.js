const Intern = require("../lib/Intern.js")

describe('Intern', () => {
    describe('Initialization', () => {
      // Positive test
      it("should be a type of Intern", () => {
        let obj = new Intern()
        expect(obj instanceof Intern).toEqual(true)
        
      });
  
      
      
    });
  });


  